extends layout

block content
  .container.not-found
    .row
    .row
      a
        img(src='images/error.jpg')
      h3 Esta não é a página que você está procurando.
      h3= error.status
      pre #{error.stack}
